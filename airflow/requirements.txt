# Airflow only needs lightweight packages for DAG orchestration
# Heavy ML packages (torch, xgboost) run inside the backend container
apache-airflow-providers-http==4.10.0
yfinance==0.2.40
pandas==2.2.2
numpy==1.26.4
requests==2.32.2
pyarrow>=14.0,<16
